# Models Directory

This directory contains trained model artifacts:

- `flower_classifier.keras` - The trained TensorFlow/Keras model
- `class_names.txt` - List of flower class names

These files are generated by running `python src/train.py`.
