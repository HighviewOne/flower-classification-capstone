[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
tensorflow = ">=2.15.0"
numpy = ">=1.24.0"
pandas = ">=2.0.0"
scikit-learn = ">=1.3.0"
pillow = ">=10.0.0"
matplotlib = ">=3.7.0"
seaborn = ">=0.12.0"
flask = ">=3.0.0"
gunicorn = ">=21.0.0"
requests = ">=2.31.0"
tqdm = ">=4.65.0"

[dev-packages]
jupyter = ">=1.0.0"
ipykernel = ">=6.25.0"
pytest = ">=7.4.0"

[requires]
python_version = "3.11"
